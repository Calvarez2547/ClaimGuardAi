# Database

This directory is reserved for database assets that support the application lifecycle outside the backend codebase itself.

- `migrations/` for schema change scripts
- `seed/` for reference data and local development seed files

